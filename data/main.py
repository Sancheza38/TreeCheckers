import pygame as pg
import constants

w = constants.GR_WIDTH
h = constants.GR_HEIGHT


def init_circles(num_x=constants.CIRCLE_COLUMNS, num_y=constants.CIRCLE_ROWS) -> List[CircleTile]:
    




def main():
    mouse_down = False
    """Main function"""
    pg.init()
    screen = pg.display.set_mode((640, 480))
    clock = pg.time.Clock()

